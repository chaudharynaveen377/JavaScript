//  check the type of triangle

const triangleType=(a,b,c)=>{
    if(a==b && b==c && c==a)return "Equileteral";
    else if(a==b || b==c || c==a)return "Isosceles"
    else{
        return "scalene";
    }
}

console.log(triangleType(2,2,2))
